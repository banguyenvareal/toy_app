<%= form_with(model: miccropost, local: true) do |form| %>
  <% if miccropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(miccropost.errors.count, "error") %> prohibited this miccropost from being saved:</h2>

      <ul>
      <% miccropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :what %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
